---
scope: Critter Plains — Visualization & Interaction Controls epic
purpose: Fix invariants for implementing PRD FR-1..FR-11 (pan/zoom, selection/inspection, speed control, status indicators) so independently-built pieces don't diverge
altitude: epic
updated: 2026-09-17T17:45
---

- (decision) Working mode: Coaching path. Deliverable: spine only, no extra rendering/artifact requested.
- (decision) AD candidate: dedicated Camera/Viewport class owns position + zoom + world<->screen transform math. Binds: SimulationController composes it instead of holding viewportX/Y/cellSize itself; BufferedWorldView, InputHandler.mapPosition, recenter (FR-6), and the selection indicator (FR-7) all read the same Camera. Prevents: four call sites each computing their own tile<->pixel math and drifting.
- (decision) AD candidate: UI overlays (memory tint FR-5, selection indicator FR-7, pause badge FR-10, tier badge FR-11) render as a separate pass after the world buffer is blitted to screen, not baked into BufferedWorldView. Binds: BufferedWorldView keeps taking only World (sim truth) as input. Prevents: world-render contract absorbing UI/interaction state.
- (decision) AD candidate: Speed Tier (1/2/3) is first-class state on SimulationController; tick interval is derived from tier x base rate, not the reverse. Binds: FR-11's indicator reads tier directly. Prevents: reverse-deriving tier from a float fps/interval value.
- (assumption) Web-verified (guide.openrndr.org, interaction/mouseAndKeyboardEvents): OPENRNDR Keyboard exposes only discrete keyDown/keyUp/keyRepeat/character events, no built-in held-key query. Mouse exposes 'scrolled' event carrying position+rotation, usable as zoom delta.
- (decision) AD candidate: continuous input (hold-to-pan) tracked via InputHandler maintaining its own held-key Set<Int>, updated on keyDown/keyUp, polled once per frame in the render loop to drive Camera.pan(). Not via OPENRNDR keyRepeat (GLFW repeat has an initial-delay + fixed-cadence stutter unsuitable for FR-1's 'smooth' requirement) and not via a nonexistent pressedKeys API. Discrete actions (select click, Escape deselect, spacebar pause, 1/2/3 tier switch, C recenter, scroll-wheel zoom) stay event-driven exactly as today, unchanged.
- (decision) Resolves PRD Open Question 5: selected critter dying mid-session auto-clears the selection (same as FR-4 deselect) — panel, memory tint, indicator, and recenter target all clear together. Prevents: stale overlay pointing at/highlighting a dead critter.
- (event) Coaching decisions complete, user approved distilling to spine.
- (change) Tech-verification reviewer + independent re-check against openrndr/openrndr source at pinned tag v0.4.5 (Keys.kt): OPENRNDR ships a built-in org.openrndr.KeyTracker(keyEvents) class with pressedKeys: Set<String>, in the same module as Keyboard. AD-4 was wrong to hand-roll a held-key Set<Int> from keyDown/keyUp — revised to use the built-in KeyTracker instead, which better satisfies AD-4's own stated Prevents clause (no invented tracking mechanism).
- (assumption) KeyTracker.pressedKeys is Set<String> (key names, e.g. "W"), not Set<Int> key codes like the existing discrete keyDown handlers use — InputHandler will compare against key-name strings for held-state checks and the existing Int KEY_W/A/S/D constants stay only for the discrete (non-held) path, if any remains.
- (change) Rubric reviewer found 2 critical + 1 high + 1 medium + 2 low findings. Fixed: (C1) AD-1/AD-2 contradiction over zoom reaching BufferedWorldView -- carved narrow exception, BufferedWorldView reads live cellSize scalar from Camera each render(), still no selection/overlay awareness. (C2) No live-refresh mechanism for selected critter across ticks -- rewrote AD-5: GameStateView gains narrow additive critter(name) lookup, SimulationController re-resolves selectedCritter every tick, clears on dead/missing. (H1) Added Visualization.kt and StateView.kt to Structural Seed as MODIFIED. (M1) AD-5 now states SimulationController.update() is the one call site that checks liveness. (L1) AD-3 states baseInterval = 1.0/initialFps. (L2) AD-5 cites PRD Open Question 5. Also carried forward AD-4's KeyTracker correction (see prior entries) and noted OPENRNDR 0.5.0 exists but pin stays 0.4.5 for this epic (Deferred).
- (change) Adversarial reviewer found 8 holes (3 critical, 3 high, 1 medium, 1 low). Findings 1-2 already covered by the rubric-driven C1/C2 fixes. Fixed the 6 new ones: (3) AD-1 now names Camera's screen-space as the map-viewport rectangle only, explicitly excluding InfoPanel, with callers required to bounds-check raw window/mouse coords first. (4) AD-1 now states the zoom strategy explicitly: fixed-size RenderTarget, redrawn at live cellSize every frame (already happens regardless of zoom), not a scaled-blit of stale content; OverlayView must derive from the same model. (5) AD-2 now mandates Drawer state (fill/stroke/strokeWeight) reset at each OverlayView sub-pass's own entry, added to Consistency Conventions too. (6) AD-4 explicitly classifies +/- zoom as discrete keyDown, not part of the held-key poll set. (7) AD-2 fixes draw order: tint -> indicator -> badges. (8) AD-3 gives SpeedTier itself sole ownership of its display-label mapping.
- (event) Second lint_spine.py pass clean after all reviewer fixes applied.
- (event) spine finalized
- (change) Self-caught after both reviewer passes: Structural Seed's Camera.kt row wrongly listed SpeedTier (AD-3 correctly assigns it to SimulationController). Fixed.
